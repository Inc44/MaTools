from module_image_trimmer import clipboard_image_trimmer

if __name__ == "__main__":
	clipboard_image_trimmer()

# compile rust for every system seperately
# (IDK how to make universal)
# Rename to match name and extension (.dll > .pyd for Windows, .so for Linux)
